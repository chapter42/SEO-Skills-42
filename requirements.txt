# 42 SEO Skills — Python Dependencies
# pip install -r requirements.txt

# === Core (used by multiple skills) ===
google-generativeai>=0.8.0    # Gemini embeddings (keyword-mapper, passage-analyzer, AI skills)
beautifulsoup4>=4.12.0        # HTML parsing (audit, technical, screaming-frog)
requests>=2.32.4              # HTTP client (all fetch operations)
lxml>=6.0.2                   # XML/HTML parsing (sitemap, schema, structured-data)
numpy>=2.0.0                  # Embedding vectors, matrix operations (similarity engine)
scipy>=1.14.0                 # Cosine similarity at scale (keyword-mapper, near-duplicates)
urllib3>=2.6.3                # HTTP low-level (security: CVE-2026-21441 fix)
validators>=0.22.0            # URL validation (sitemap, technical)
Pillow>=12.1.0                # Image analysis (images)

# === Optional — install as needed ===
# playwright>=1.56.0          # Browser automation (internal-links, seo-agi)
#                              # Also run: playwright install
# reportlab>=4.0               # PDF report generation (geo-report --pdf)
# textstat>=0.7.0              # Readability scoring (readability)
# networkx>=3.0                # Link graph analysis (link-graph)
# rapidfuzz>=3.0.0             # Fuzzy string matching (product-titles, ecom-taxonomy)
# spacy>=3.7.0                 # Named entity recognition (entity-extractor)
#                              # Also run: python -m spacy download en_core_web_sm
# nltk>=3.9.0                  # N-gram extraction (ecom-taxonomy)
# plotly>=5.0.0                # Interactive charts (content-decay, share-of-voice)
# google-auth>=2.0.0           # GSC API authentication (seo-agi)
# google-api-python-client>=2.0.0  # GSC API client (seo-agi)
